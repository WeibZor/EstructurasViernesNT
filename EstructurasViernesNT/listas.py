# Necesito crear un programa
# que almacena una lista de 200 notas. 

import random
notas = []
for i in range(200):
    notaSimulada = random.randint(1,5)
    notas.append(notaSimulada)
    
#print(notas)
#notas.insert(0,80)
#print(notas) #Pone un número insertado a la fuerza
#notas.remove(1)
#print(notas) #Elmina el valor númerico
#notas.sort(reverse=True)
#notas.clear() #Borra información
print(notas) 

# Metodos para manipular o transformar, modificar o administrar listas

    


    